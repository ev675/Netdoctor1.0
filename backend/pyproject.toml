[project]
name = "netdoctor-backend"
version = "0.1.0"
description = "NetDoctor intelligent internet fault diagnosis backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
]

[tool.fastapi]
entrypoint = "app.main:app"

[dependency-groups]
dev = [
    "pytest",
    "httpx",
]