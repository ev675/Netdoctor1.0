services:

  postgres:
    image: postgres:18
    container_name: netdoctor-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: netdoctor
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: